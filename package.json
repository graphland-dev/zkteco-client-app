{
  "name": "att",
  "version": "1.0.0",
  "description": "ZKTeco attendance device integration",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun run app.ts",
    "typecheck": "bun run --filter @graphland/zk-client typecheck",
    "test": "bun run --filter @graphland/zk-client test"
  },
  "dependencies": {
    "@graphland/zk-client": "workspace:*"
  }
}
